<form method="post" action="{{ path('lecturers_delete', {'currentDepartment' : department_name,'id': lecturer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lecturer.id) }}">
    <button class="btn">Delete</button>
</form>
